<div class="row">
<div class="alert alert-dark col-5　d-flex" role="alert">
    <strong>カテゴリー編集</strong>
</div>
</div>

<div class="row">
 <%= form_with model: @category, url: admin_category_path, local:true do |f| %>
  <span>カテゴリー名</span>
  <span><%= f.text_field :name %></span>
  <span><%= f.submit '変更を保存', class:"btn btn-success" %></span>
 <% end %>
</div>
